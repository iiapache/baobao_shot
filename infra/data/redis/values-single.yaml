# Redis 7 — dev / staging 单节点

architecture: standalone

master:
  persistence:
    size: 5Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi

replica:
  replicaCount: 0

sentinel:
  enabled: false
