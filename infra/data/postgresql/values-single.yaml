# PostgreSQL 15 — dev / staging 单节点
# 覆盖 values.yaml，关闭主备复制

architecture: standalone

readReplicas:
  replicaCount: 0

primary:
  persistence:
    size: 20Gi
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 1
      memory: 2Gi
