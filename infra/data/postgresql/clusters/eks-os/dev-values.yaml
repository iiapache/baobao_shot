# EKS 新加坡 — dev 命名空间 PostgreSQL 覆盖
global:
  postgresql:
    auth:
      existingSecret: baobao-postgresql

commonLabels:
  baobao.io/cluster: eks-os
  baobao.io/region: os
  baobao.io/environment: dev

primary:
  persistence:
    storageClass: gp3
