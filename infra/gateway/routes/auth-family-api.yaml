# auth-family-svc 网关路由 — T1.12
#
# JWT：forward-auth → auth-family-svc /internal/verify（HS256 + 黑名单，与 T1.3 一致）
# 公开路由：/v1/auth/* 无需 Access Token；/v1/auth/refresh 直通
# 受保护路由：/v1/account/*、/v1/families/*、/v1/invitations/* 需 JWT
# 限流：design-api §2.7，详见 infra/gateway/docs/rate-limits.md
#
# kubectl apply -f infra/gateway/routes/auth-family-api.yaml -n dev

---
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: auth-family-api
  labels:
    app.kubernetes.io/part-of: baobao
    app.kubernetes.io/component: gateway-route
    baobao.io/upstream: auth-family-svc
    baobao.io/task: T1.12
spec:
  ingressClassName: apisix
  http:
    # --- 公开：刷新 Token（无需 Access Token）---
    - name: auth-refresh
      priority: 96
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/auth/refresh
        methods:
          - POST
      backends:
        - serviceName: auth-family-svc
          servicePort: 80

    # --- 公开：短信验证码（限流 60s/3 + 1h/10，维度 手机号+IP）---
    - name: auth-phone-code
      priority: 96
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/auth/phone/code
        methods:
          - POST
      backends:
        - serviceName: auth-family-svc
          servicePort: 80
      plugins:
        - name: serverless-pre-function
          enable: true
          config:
            phase: rewrite
            functions:
              - |
                return function(conf, ctx)
                  local core = require("apisix.core")
                  local cjson = require("cjson.safe")
                  local body = core.request.get_body()
                  if body then
                    local data = cjson.decode(body)
                    if data and data.phone then
                      core.request.set_header("X-Baobao-Rate-Phone", tostring(data.phone))
                    end
                  end
                end
        - name: limit-count
          enable: true
          config:
            count: 3
            time_window: 60
            key_type: var_combination
            key: "$http_x_baobao_rate_phone $remote_addr"
            rejected_code: 429
            rejected_msg: '{"code":"COMMON_RATE_LIMIT","message":"rate limit exceeded"}'
            policy: local
        - name: limit-count
          enable: true
          config:
            count: 10
            time_window: 3600
            key_type: var_combination
            key: "$http_x_baobao_rate_phone $remote_addr"
            rejected_code: 429
            rejected_msg: '{"code":"COMMON_RATE_LIMIT","message":"rate limit exceeded"}'
            policy: local

    # --- 公开：手机号登录（限流 60s/5，维度 手机号+IP）---
    - name: auth-phone-login
      priority: 96
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/auth/phone/login
        methods:
          - POST
      backends:
        - serviceName: auth-family-svc
          servicePort: 80
      plugins:
        - name: serverless-pre-function
          enable: true
          config:
            phase: rewrite
            functions:
              - |
                return function(conf, ctx)
                  local core = require("apisix.core")
                  local cjson = require("cjson.safe")
                  local body = core.request.get_body()
                  if body then
                    local data = cjson.decode(body)
                    if data and data.phone then
                      core.request.set_header("X-Baobao-Rate-Phone", tostring(data.phone))
                    end
                  end
                end
        - name: limit-count
          enable: true
          config:
            count: 5
            time_window: 60
            key_type: var_combination
            key: "$http_x_baobao_rate_phone $remote_addr"
            rejected_code: 429
            rejected_msg: '{"code":"COMMON_RATE_LIMIT","message":"rate limit exceeded"}'
            policy: local

    # --- 公开：Apple 登录 ---
    - name: auth-apple
      priority: 96
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/auth/apple
        methods:
          - POST
      backends:
        - serviceName: auth-family-svc
          servicePort: 80

    # --- 受保护：账号 ---
    - name: auth-account-protected
      priority: 85
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/account/*
      backends:
        - serviceName: auth-family-svc
          servicePort: 80
      plugins:
        - name: forward-auth
          enable: true
          config:
            uri: "http://auth-family-svc:80/internal/verify"
            request_headers:
              - Authorization
              - X-Request-Id
              - X-Trace-Id
            upstream_headers:
              - X-User-Id
              - X-Token-Jti
            client_headers:
              - X-User-Id
            status_on_error: 401

    # --- 受保护：家庭 ---
    - name: auth-families-protected
      priority: 85
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/families
          - /v1/families/*
      backends:
        - serviceName: auth-family-svc
          servicePort: 80
      plugins:
        - name: forward-auth
          enable: true
          config:
            uri: "http://auth-family-svc:80/internal/verify"
            request_headers:
              - Authorization
              - X-Request-Id
              - X-Trace-Id
            upstream_headers:
              - X-User-Id
              - X-Token-Jti
            client_headers:
              - X-User-Id
            status_on_error: 401

    # --- 受保护：邀请加入 ---
    - name: auth-invitations-protected
      priority: 85
      match:
        hosts:
          - dev-api-cn.example.com
        paths:
          - /v1/invitations/*
      backends:
        - serviceName: auth-family-svc
          servicePort: 80
      plugins:
        - name: forward-auth
          enable: true
          config:
            uri: "http://auth-family-svc:80/internal/verify"
            request_headers:
              - Authorization
              - X-Request-Id
              - X-Trace-Id
            upstream_headers:
              - X-User-Id
              - X-Token-Jti
            client_headers:
              - X-User-Id
            status_on_error: 401
