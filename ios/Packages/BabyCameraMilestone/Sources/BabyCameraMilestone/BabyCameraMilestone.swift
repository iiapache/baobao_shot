/// BabyCameraMilestone — 内置里程碑目录、本地通知调度、自定义 CRUD 与日历联动（T2.19 / T2.20）。
public enum BabyCameraMilestone {
    public static let version = "0.2.0"
}

/// 里程碑列表 + 日历组合视图（内置 + 自定义）。
public typealias MilestoneListView = CustomMilestoneListView
