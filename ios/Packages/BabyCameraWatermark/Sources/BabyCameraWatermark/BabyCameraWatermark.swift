/// BabyCameraWatermark — 品牌水印与深度合成角标渲染（T2.16 / T3.25）
///
/// 核心类型：`WatermarkRenderer`、`BrandWatermarkPolicy`、`BrandWatermarkLayout`、
/// `DeepSynthesisBadgeLayout`、`CameraWatermarkHooks`（接入 `BabyCameraCamera` 烧录 hook）。
public enum BabyCameraWatermark {
    public static let version = "0.2.0"
}
