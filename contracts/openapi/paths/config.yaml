# design-backend.md config-svc — 灰度与运营配置
paths:
  /v1/config/features:
    get:
      tags: [config]
      summary: 获取 Feature Flags（按 region / userIdHash 分流）
      operationId: configGetFeatures
      security: []
      parameters:
        - $ref: '../components/common.yaml#/components/parameters/HeaderXRegion'
        - $ref: '../components/common.yaml#/components/parameters/HeaderXAppVersion'
        - $ref: '../components/common.yaml#/components/parameters/HeaderXDeviceId'
        - name: X-User-Id-Hash
          in: header
          required: false
          description: 客户端预计算 userId 分桶（0–99）；未传时服务端根据 JWT sub 计算
          schema:
            type: integer
            minimum: 0
            maximum: 99
        - name: Authorization
          in: header
          required: false
          description: 可选 Bearer JWT，用于 userIdHash 分桶
          schema:
            type: string
      responses:
        '200':
          description: Feature flags 列表
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '../components/common.yaml#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/FeatureFlagsPayload'
        '400':
          $ref: '../components/common.yaml#/components/responses/BadRequest'

  /v1/admin/features/{key}:
    patch:
      tags: [config]
      summary: 紧急更新 feature flag（T7.14 kill-switch）
      operationId: configAdminPatchFeature
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
            example: ai.play.ghibli_kid
        - name: X-Admin-Token
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                defaultEnabled:
                  type: boolean
                rolloutPercent:
                  type: integer
                  minimum: 0
                  maximum: 100
                variant:
                  type: string
      responses:
        '200':
          $ref: '../components/common.yaml#/components/responses/Ok'
        '401':
          $ref: '../components/common.yaml#/components/responses/Unauthorized'
        '404':
          $ref: '../components/common.yaml#/components/responses/NotFound'

  /v1/admin/plays/{id}:
    patch:
      tags: [config]
      summary: 紧急更新玩法目录项（T7.14 kill-switch）
      operationId: configAdminPatchPlay
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            example: play_storybook
        - name: X-Admin-Token
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
      responses:
        '200':
          $ref: '../components/common.yaml#/components/responses/Ok'
        '401':
          $ref: '../components/common.yaml#/components/responses/Unauthorized'
        '404':
          $ref: '../components/common.yaml#/components/responses/NotFound'

  /v1/config/plays:
    get:
      tags: [config]
      summary: 玩法目录（config-svc 占位，与 ai-dispatch 目录可并存）
      operationId: configListPlays
      security: []
      parameters:
        - $ref: '../components/common.yaml#/components/parameters/HeaderXRegion'
        - $ref: '../components/common.yaml#/components/parameters/HeaderXAppVersion'
        - $ref: '../components/common.yaml#/components/parameters/HeaderXDeviceId'
      responses:
        '200':
          description: 玩法目录
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '../components/common.yaml#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/PlaysCatalogPayload'

components:
  schemas:
    FeatureFlagsPayload:
      type: object
      required: [version, ttlSeconds, context, features]
      properties:
        version:
          type: string
          example: "20250606001"
        ttlSeconds:
          type: integer
          example: 300
        context:
          $ref: '#/components/schemas/FeatureEvalContext'
        features:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FeatureFlagResult'

    FeatureEvalContext:
      type: object
      required: [region, userIdHash]
      properties:
        region:
          type: string
          enum: [cn, os]
        appVersion:
          type: string
        userIdHash:
          type: integer
          minimum: 0
          maximum: 99

    FeatureFlagResult:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean
        variant:
          type: string
          nullable: true
        rolloutPercent:
          type: integer
          minimum: 0
          maximum: 100
          description: 配置的目标放量（rollout.* keys，T7.14）

    PlaysCatalogPayload:
      type: object
      required: [version, plays, source]
      properties:
        version:
          type: string
        source:
          type: string
          example: config-svc
        plays:
          type: array
          items:
            $ref: '#/components/schemas/PlayCatalogItem'

    PlayCatalogItem:
      type: object
      required: [id, name, enabled]
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        regions:
          type: array
          items:
            type: string
            enum: [cn, os]
        enabled:
          type: boolean
